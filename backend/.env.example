# --------------------------------------------------
# Backend Environment Variables
# --------------------------------------------------
# Copy this file to `.env` and fill in real values.
# NEVER commit the `.env` file to GitHub.
# --------------------------------------------------

# Port number for the backend server (local development)
PORT=5000

# --------------------------------------------------
# AI Provider Configuration
# --------------------------------------------------
# OpenRouter API key
# Required for AI responses via OpenRouter
# (You may replace this with OpenAI, Cohere, etc.)
# --------------------------------------------------

OPENROUTER_API_KEY=your_openrouter_api_key_here


# --------------------------------------------------
# Database Configuration
# --------------------------------------------------
# MongoDB connection string (Local or Atlas)
# Use ONE of the following formats
# --------------------------------------------------

# Local MongoDB example
MONGODB_URI=mongodb://localhost:27017/your-database-name

# MongoDB Atlas example (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/your-database-name


# --------------------------------------------------
# Email Configuration — v1.2 (Email Verification)
# --------------------------------------------------
# LGC Concept AI uses an HTTP-based email API instead of SMTP.
#
# Why NOT SMTP?
# - Cloud platforms (Railway, Vercel, etc.) often block SMTP ports
# - SMTP causes connection timeouts (ETIMEDOUT) in production
# - HTTP-based email APIs are more reliable and cloud-friendly
#
# Provider used:
# - Resend (https://resend.com)
#
# Used for:
# - Account email verification
# - Resend verification links
# - (Future) Password reset
# --------------------------------------------------

# Resend API key (required)
RESEND_API_KEY=your_resend_api_key_here


# --------------------------------------------------
# Frontend URL Configuration
# --------------------------------------------------
# Base URL of the frontend application.
# Used by the backend to generate links
# (e.g., email verification links).
#
# The verification email will contain:
#   {FRONTEND_URL}/verify-email?token=...
#
# Example (local development):
#   http://localhost:5173
#
# Example (production):
#   https://your_frontend_domain.com
# --------------------------------------------------

FRONTEND_URL=http://localhost:5173
