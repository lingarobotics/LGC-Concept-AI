# --------------------------------------------------
# Backend Environment Variables
# --------------------------------------------------
# Copy this file to `.env` and fill in real values.
# NEVER commit the `.env` file to GitHub.
# --------------------------------------------------


# --------------------------------------------------
# Server
# --------------------------------------------------

# Port number for the backend server
PORT=5000


# --------------------------------------------------
# Database
# --------------------------------------------------

# MongoDB connection string
# Use ONE of the following formats

# Local MongoDB example
MONGODB_URI=mongodb://localhost:27017/your-database-name

# MongoDB Atlas example (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/your-database-name


# --------------------------------------------------
# AI Provider
# --------------------------------------------------

# OpenRouter API key
# Required for AI responses (Learn / Doubt / Teach-back modes)
# You can get a free API key from https://openrouter.ai
# Or you can use any provider's API key with appropriate modifications in the codebase.
OPENROUTER_API_KEY=your_openrouter_api_key_here


# --------------------------------------------------
# Email Configuration (v1.2)
# --------------------------------------------------
# Brevo SMTP is used for email verification and
# transactional emails.
#
# Reason:
# - No Gmail rate limits
# - Designed for production transactional emails
# - Works well on Railway / cloud platforms
# - Free tier sufficient for early-stage products
#
# Note:
# - Domain verification is recommended for production
#   to avoid spam warnings and improve deliverability.
# --------------------------------------------------

# SMTP server host (Brevo)
SMTP__HOST=smtp-relay.brevo.com

# SMTP port (587 = TLS, recommended)
SMTP__PORT=587

# Brevo SMTP login (provided by Brevo)
# Example: xxxxx@smtp-brevo.com
SMTP__USER=your_smtp_login@smtp-brevo.com

# Brevo SMTP password (NOT your Brevo account password)
SMTP__PASS=your_smtp_password_here

# Sender name and email shown to users
# For best deliverability, this should match
# a verified sender or domain in Brevo
SMTP__FROM=LGC Concept AI <no-reply@your-domain.com>


# --------------------------------------------------
# Frontend URL
# --------------------------------------------------
# Base URL of the frontend application.
# Used to generate verification links.
#
# Example (local):
# http://localhost:5173
#
# Example (production):
# https://lgc-concept-ai.vercel.app
# --------------------------------------------------

FRONTEND_URL=http://localhost:5173
