# --------------------------------------------------
# Backend Environment Variables
# --------------------------------------------------
# Copy this file to `.env` and fill in real values.
# NEVER commit the `.env` file to GitHub.
# --------------------------------------------------

# --------------------------------------------------
# Server
# --------------------------------------------------

# Port number for the backend server
PORT=5000


# --------------------------------------------------
# Database
# --------------------------------------------------

# MongoDB connection string
# Use ONE of the following formats

# Local MongoDB example
MONGODB_URI=mongodb://localhost:27017/your-database-name

# MongoDB Atlas example (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/your-database-name


# --------------------------------------------------
# AI Provider
# --------------------------------------------------

# OpenRouter API key
# Required for AI responses (Learn / Doubt / Teach-back modes)
OPENROUTER_API_KEY=your_openrouter_api_key_here


# --------------------------------------------------
# Email Configuration (v1.2)
# --------------------------------------------------
# Gmail SMTP is used for email verification and
# transactional emails in early-stage (v1.x).
#
# Reason:
# - No domain required
# - No sandbox restrictions
# - Zero cost
# - Reliable for low-to-moderate traffic
#
# This can be migrated to SES / Resend / SendGrid
# once a custom domain is available.
# --------------------------------------------------

# SMTP server host (Gmail)
SMTP_HOST=smtp.gmail.com

# SMTP port (587 = TLS, recommended)
SMTP_PORT=587

# Gmail address used to send emails
SMTP_USER=example@gmail.com

# Google App Password (16 characters, NO spaces)
# Generated from Google Account → Security → App passwords
SMTP_PASS=your_16_char_app_password_here

# Sender name and email shown to users
SMTP_FROM=LGC Concept AI <example@gmail.com>


# --------------------------------------------------
# Frontend URL
# --------------------------------------------------
# Base URL of the frontend application.
# Used to generate verification links.
#
# Example (local):
# http://localhost:5173
#
# Example (production):
# https://frontend-domain.com
# --------------------------------------------------

FRONTEND_URL=http://localhost:5173
