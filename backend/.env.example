# --------------------------------------------------
# Backend Environment Variables
# --------------------------------------------------
# Copy this file to `.env` and fill in real values.
# NEVER commit the `.env` file to GitHub.
# --------------------------------------------------


# --------------------------------------------------
# Server
# --------------------------------------------------

# Port number for the backend server
PORT=5000


# --------------------------------------------------
# Database
# --------------------------------------------------

# MongoDB connection string
# Use ONE of the following formats

# Local MongoDB example
MONGODB_URI=mongodb://localhost:27017/your-database-name

# MongoDB Atlas example (cloud)
# Replace <username>, <password>, <cluster>, <database> with real values
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>



# --------------------------------------------------
# AI Providers (Unified Fallback Architecture)
# --------------------------------------------------
# All AI modes use the SAME fallback stack:
#
# 1. Llama 3.3 70B (OpenRouter Key 1)
# 2. Nemotron 30B (OpenRouter Key 2)
# 3. Gemma 27B (OpenRouter Key 2)
# 4. Gemini 2.5 Flash (Direct Google API)
#
# If one model fails (429 / timeout / error),
# the system automatically falls back to the next model.

# OpenRouter Primary Key
OPENROUTER_API_KEY_1=your_openrouter_primary_key_here

# OpenRouter Secondary Key
OPENROUTER_API_KEY_2=your_openrouter_secondary_key_here

# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here



# --------------------------------------------------
# Email (Brevo HTTP API)
# --------------------------------------------------

# Brevo API key
BREVO_API_KEY=your_brevo_api_key_here

# Verified sender email in Brevo
BREVO_SENDER_EMAIL=admin@your-domain.com



# --------------------------------------------------
# Frontend URL
# --------------------------------------------------

# Base URL of frontend application
# Used for verification and password reset links
FRONTEND_URL=http://localhost:5173