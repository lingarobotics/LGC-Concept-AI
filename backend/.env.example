# --------------------------------------------------
# Backend Environment Variables
# --------------------------------------------------
# Copy this file to `.env` and fill in real values.
# NEVER commit the `.env` file to GitHub.
# --------------------------------------------------


# --------------------------------------------------
# Server
# --------------------------------------------------

# Port number for the backend server
PORT=5000


# --------------------------------------------------
# Database
# --------------------------------------------------

# MongoDB connection string
# Use ONE of the following formats

# Local MongoDB example
MONGODB_URI=mongodb://localhost:27017/your-database-name

# MongoDB Atlas example (cloud)
# Replace <username>, <password>, <cluster>, <database> with real values
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>



# --------------------------------------------------
# AI Provider
# --------------------------------------------------

# OpenRouter API key
# Required for AI responses (Learn / Doubt / Teach-back modes)
# You can get a free API key from https://openrouter.ai
# You may replace this with any AI provider if your codebase supports it.
OPENROUTER_API_KEY=your_openrouter_api_key_here



# --------------------------------------------------
# Email (Brevo HTTP API)
# --------------------------------------------------
# Used for email verification and transactional emails.
#
# Why Brevo HTTP API?
# - No SMTP restrictions
# - No sandbox blocking after sender verification
# - Free tier sufficient for early-stage products
#
# Email Setup Notes:
# - You can verify and use a single email address in Brevo.
# - For production systems, authenticating a domain is strongly recommended.
# - Configure SPF, DKIM, and DMARC for better deliverability.
# --------------------------------------------------

# Brevo API key (keep secret)
BREVO_API_KEY=your_brevo_api_key_here

# Verified sender email in Brevo
# Example:
# admin@your-domain.com
# support@your-domain.com
BREVO_SENDER_EMAIL=admin@your-domain.com



# --------------------------------------------------
# Frontend URL
# --------------------------------------------------
# Base URL of the frontend application.
# Used to generate verification and password reset links.
#
# Example (local):
# http://localhost:5173
#
# Example (production):
# https://your-frontend-domain.com
# --------------------------------------------------

FRONTEND_URL=http://localhost:5173
